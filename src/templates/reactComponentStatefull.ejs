import React from 'react';
import { Container } from './styles';

class <%= props.componentName %> extends React.Component {
  render() {
    return (
      <Container>
      </Container>
    );
  }
};

export default <%= props.componentName %>;