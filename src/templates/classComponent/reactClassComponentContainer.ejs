import React from 'react';

class <%= props.capitalizedComponentName %>Container extends React.Component {
  render() {
    return (
      <>
        {React.cloneElement(children, {})}
      </>
    );
  }
};

export default <%= props.capitalizedComponentName %>Container;